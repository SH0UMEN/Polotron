const fs = require('fs'),
      g2 = require('gradient2');

const state =  {

};

const mutations = {
    setFilename(state, name) {
        state.filename = name;
    }
};

const actions = {

};

const getters = {

};

export default {
    state,
    getters,
    mutations,
    actions
}